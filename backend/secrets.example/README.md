Create real local secrets in `backend/.secrets/` before starting the backend or Docker Compose stack.

Recommended files:

- `jwt_secret_key.txt`
- `admin_password.txt`
- `device_keys.json`

Use `D:\CODE\Projects\New folder\AnomalyGuard\scripts\bootstrap-local-secrets.ps1` to generate them.
